// PLACEHOLDER SO I CAN GET THE CORE STUFF WORKING
//
// todo: add all the vector stuff (add, sub, normalize, etc etc)

export class Vector2 {
	public x: number;
	public y: number;

	constructor(x: number, y: number) {
		this.x = x;
		this.y = y;
	}
}
